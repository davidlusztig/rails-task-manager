<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
  <li><%= link_to task.title, task_path(task) %></li>
  <span><%= link_to task.completed, task_path(task) %></span>
  <% end %>
</ul>
<p> <%= link_to "Add a new task", tasks_new_path %> </p>
